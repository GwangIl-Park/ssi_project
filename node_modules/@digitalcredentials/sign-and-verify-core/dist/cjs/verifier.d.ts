import { DIDDocument } from "./types";
import { SignatureOptions } from "./signatures";
interface VerifyCredentialParameters {
    verifiableCredential: any;
    issuerMembershipRegistry: any;
    options?: SignatureOptions;
}
interface VerifyPresentationParameters {
    verifiablePresentation: any;
    issuerMembershipRegistry: any;
    options?: SignatureOptions;
}
export declare const validateCredential: (verifiableCredential: any, issuerMembershipRegistry: any) => Promise<boolean>;
export declare const validatePresentation: (verifiablePresentation: any, issuerMembershipRegistry: any) => Promise<boolean>;
export declare const createVerifier: (preloadedDidDocuments: DIDDocument[]) => {
    verify: ({ verifiableCredential, issuerMembershipRegistry, options }: VerifyCredentialParameters) => Promise<any>;
    verifyPresentation: ({ verifiablePresentation, issuerMembershipRegistry, options }: VerifyPresentationParameters) => Promise<any>;
};
export {};
